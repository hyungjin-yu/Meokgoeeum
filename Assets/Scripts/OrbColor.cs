/// <summary>
/// OrbColor (색 구슬의 색)
/// [[04 색 구슬 시스템]] 기준 6색. 인덱스 순서가 배열 기반 카운트(ColorSystemManager)에
/// 그대로 쓰이므로 순서를 바꾸지 말 것.
/// </summary>
public enum OrbColor
{
    Red,    // 빨강 - 강타
    Blue,   // 파랑 - 흐름
    Yellow, // 노랑 - 번쩍
    Green,  // 초록 - 되살림
    Purple, // 보라 - 왜곡
    Black,  // 검정 - 먹물
}
