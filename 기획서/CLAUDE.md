# 먹괴음 (Meokgoeeum) — 개인 프로젝트

## 프로젝트 개요

- **목적/목표:** Steam PC 출시용 3D 실시간 액션 RPG 완성
- **플랫폼:** PC (Steam)
- **완성 기준 (MVP):** v0.1 — 이동 + 전투 + 색 복원 파동 + 먹괴음 1종이 돌아가는 프로토타입
- **팀:** 1인 개발 (기획·프로그래밍) + 디자이너 친구 합류 협의 중

## 기술 스택

- **엔진:** Unity 6.3 LTS
- **렌더 파이프라인:** URP (Universal Render Pipeline)
- **주요 패키지:** Addressables, New Input System, Cinemachine, VFX Graph, Timeline, NavMesh, Unity Behavior (Preview)
- **서드파티:** DOTween, UniTask
- **언어:** C# 전용 (Blueprint 없음)
- **세이브:** JsonUtility → `Application.persistentDataPath`

## 엔진 선택 배경 (중요)

- UE5 → Unity 6.3 LTS로 전환 결정 (2026-05-29)
- **전환 이유:** 1인 개발 + C# 친숙 + Asset Store + 빠른 이터레이션
- **URP 선택 이유:** HDRP는 1인 개발에 설정 부담이 크고, 이 게임의 핵심 연출(색 복원 파동)은 URP Shader Graph로 충분
- Lumen(UE5) → APV + Shader Graph로 대체. 스타일라이즈드 아트 방향으로 한계 희석

## 게임 핵심 컨셉 (빠른 파악용)

```
장르:  3D 실시간 액션 RPG
배경:  조선 풍 현대 도시 (회색조 → 색 복원)
주인공: 붓을 무기로 쓰는 성인 (어린 시절 기억을 잃은 상태)
적:    먹괴음 — 먹물 괴물 5종 (평/원/흡/분/광)
핵심:  적 처치 → 색 구슬 드랍 → 파동 연출로 색 복원 → 맵 회전
반전:  이 세계는 큐브 1개. 탑이 아니라 큐브 면을 순환하는 구조
보스:  포(鯆) — 핑크 돌고래, 3페이즈, 플레이어의 옛 친구
```

## 기획 문서 구조 (31개 파일)

| 번호 | 파일 | 내용 |
|------|------|------|
| 00 | 게임 개요 | 한 줄 요약, 장르, 플랫폼, 핵심 루프 |
| 01 | 세계관 & 스토리 | 배경, 플레이어 정체, 스토리 흐름 |
| 02 | 플레이어 시스템 | 스탯, 전투 구조, 스킬, 성장 시스템 |
| 03 | 먹괴음 - 적 설계 | 5종 특성, 층별 등장, 강화 버전 |
| 04 | 색 구슬 시스템 | 드랍, 파동 연출, 6색, 도감 |
| 05 | 맵 시스템 - 큐브 구조 | 큐브 6면, 맵 회전, 층 구조 |
| 06 | 보스 설계 | 포(鯆) 3페이즈 패턴, 약점, 엔딩 |
| 07 | UI & 아트 방향 | HUD 레이아웃, 스타일, 폰트 |
| 08 | 사운드 기획 | BGM 방향, SFX, AudioMixer |
| 09 | 개발 마일스톤 | v0.1~v1.0 체크리스트 |
| 10 | 씬 구조 & 기술 스펙 | **Unity 핵심 클래스 구조 총정리** |
| 11 | 셰이더 설계 - 색 복원 파동 | Shader Graph + VFX Graph + C# 제어 코드 |
| 12 | 큐브 좌표계 설계 | Addressables 면 관리, 회전 로직 C# |
| 13 | 먹괴음 AI 설계 | NavMesh + Unity Behavior, 커스텀 쿼리 |
| 14 | 밸런스 수치 시트 | 플레이어/적/보스 스탯 (미확정) |
| 15 | 튜토리얼 설계 | 환경 유도 방식, 단계별 설계 |
| 16 | 조작 설계 | New Input System 키 바인딩 |
| 17 | 게임오버 & 리트라이 | 처음부터 재시작, 도감 기록 유지 |
| 18 | 세이브 & 로드 기획 | SaveData 구조, 슬롯 3개, Steam Cloud |
| 19 | 층별 상세 설계 | 1~12층 + 보스 방 상세 |
| 20 | 컷씬 & 연출 기획 | Timeline 기반, 내레이션 대사 전문 |
| 21 | 스팀 페이지 기획 | 설명문, 태그, 가격, 트레일러 계획 |
| 22 | 핵심 시스템 통합 기획 | 큐브 + 색 복원 연계 전체 타임라인 |
| 23 | UI 디자인 기획서 | 컬러 팔레트, 컴포넌트, Stitch AI 프롬프트 |
| 24 | 디자인 필러 | 4대 핵심 가치, 필러 충돌 우선순위 |
| 25 | 색맹 접근성 설계 | 색-아이콘 고정 매핑, 색맹 모드, 검증 계획 |
| 26 | 경쟁작 비교 & 포지셔닝 | Concrete Genie/Kena/Gris/Tunic 비교, SWOT |
| 27 | 전투 프레임 데이터 | Startup/Active/Recovery, 적 텔레그래프 타이밍 |
| 28 | 레벨 디자인 - 인카운터 페이싱과 블록아웃 | 긴장도 곡선, 방-노드 다이어그램 표준 |
| 29 | 개발 리스크 진단 & 스코프 컷 계획 | 리스크 레지스터, MoSCoW, 최소 출시 버전 |
| 30 | 플레이테스트 & 밸런싱 검증 계획 | 테스트 단계, 텔레메트리, 설문 템플릿 |
| 31 | 엘리베이터 피치 (1페이지 요약) | 심사/피칭용 1페이지 요약 |
| 32 | QA 리뷰 - 기획자 3인 | 시스템/레벨/내러티브 3개 관점 교차검증, 우선순위표 |

> 기술 구현 시 먼저 읽어야 할 파일: **10 → 11 → 12 → 13 → 22 순서**
> 기획 방향/판단이 헷갈릴 때 먼저 읽어야 할 파일: **24 (디자인 필러) → 29 (스코프 컷 계획)**

## 현재 개발 상태 (2026-08-16 기준)

- [x] 기획 완료 (23개 문서 → **31개 문서**, 24~31 추가: 디자인 필러/색맹 접근성/경쟁작 비교/전투 프레임 데이터/레벨 디자인 페이싱/리스크·스코프컷/플레이테스트 계획/엘리베이터 피치)
- [x] Unity 6.3 LTS URP 기술 스펙 확정
- [x] Unity 프로젝트 생성
- [x] 필수 패키지 설치 (Cinemachine, Input System, VFX Graph, Addressables, AI Navigation, Unity Behavior, UniTask, DOTween)
- [x] PlayerController — 카메라 기준 이동(camera-relative) + 이동 방향 회전 + 중력으로 재작성, `Look` 입력 액션 추가, Player 태그 수정
- [x] **Cinemachine 3인칭 카메라 연결 완료** — 실제 작동 확인됨 (Gain=20), [[logic/Cinemachine 3인칭 카메라 설정]]에 트러블슈팅까지 기록
- [x] 마우스 감도 설정 시스템 — `MouseSensitivitySetting.cs`, 나중에 설정 UI 슬라이더만 연결하면 됨 (→ [[logic/마우스 감도 설정]])
- [x] 붓 기본 공격 3타 콤보 — `BrushWeapon.cs`, `Player`에 부착 확인 완료. ⚠️ (2026-08-21 발견·수정) v0.1부터 자기 자신도 매 타격마다 같이 때리던 버그 있었음 — 자세한 내용은 [[changelog/2026-08-21_붓공격-자해버그]] 참고
- [x] 먹괴음 - 평 1종 — `EnemyPyeong.cs` + `EnemyHealth.cs` + `PlayerHealth.cs`, 실제 작동 확인됨 (Player 3클릭 처치)
- [x] 색 구슬 드랍 & 획득 — `OrbColor.cs`, `ColorSystemManager.cs`, `ColorOrbPool.cs`, `ColorOrbPickup.cs`, 실제 작동 확인됨 (드랍 → 획득 → 보유/누적 카운트까지 로그로 검증)
- [x] **색 복원 파동 코드 완료 + 타이밍 조정 + 검증 완료** — `PaintableObject.cs` + `ColorWaveEffect.cs`, C# 코루틴 기반 대체 구현(Shader Graph 원안은 [[refactor/색 복원 파동 - Shader Graph 업그레이드]]로 미룸). "동시에 칠해짐" 리포트 조사 결과 버그 아님(트리거는 거리순 정확) → `fadeDuration` 0.5→0.2초로 조정, Inspector 반영 후 순서대로 퍼지는 것 확인됨
- [x] **v0.1 목표 ①~⑤ 코드 + 실제 테스트 전부 완료.** 🎉 v0.1 프로토타입(이동/카메라, 붓 콤보, 적 AI, 색 구슬, 색 복원 파동) 전체 검증 끝
- [x] 구르기(회피) — `PlayerDodge.cs`, 평 밸런스 이슈 근본 해결, Unity 검증 + 사용자 만족 확인 완료
- [x] **색 스킬 3종(강타/흐름/번쩍) 완료 + 검증 끝** — `ColorSkillController.cs`, `ColorSystemManager.TryConsumeOrb()`, `IKnockbackable.cs` + `EnemyPyeong` 넉백. 실제 적 대상 테스트까지 "제대로 잘 작동됨" 확인. 강타의 강제 전방 이동은 기획서 스펙대로의 의도된 동작
- [x] **먹괴음 3종 추가 완료 + 검증 끝** — `EnemyWon.cs`(원거리, 먹물 투척 `InkProjectile.cs`), `EnemyBun.cs`(분열, 처치 시 미니언 2마리 자기 복제), `EnemyHeup.cs`(흡수, 비공격 + `RestoredAreaRegistry.cs`로 색 복원 구역 찾아가 회복). Unity에서 "제대로 나오네"로 확인
- [x] **큐브 좌표계 + 페이드 트랜지션 완료 + Unity 검증 끝** — `CubeMapManager.cs`, `GameState.cs`, `CubeFaceData.cs`, `FadeManager.cs`. `SC_Game`/`SC_Face_0`/`SC_Face_1` 씬 분리 + Addressables 등록까지 실제로 마치고 F4로 `0면→4면` 회전 성공 확인(페이드+씬 전환+텔레포트 전부 정상). 실전 테스트로 버그 2개 발견 즉시 수정(초기 로드 텔레포트 누락, 미설정 면 회전 시도). **아직 SC_Face_2~5는 안 만듦 — 콘텐츠 채우는 건 나중 작업**
- [x] **계단 & 층 구조 완료 + Unity 검증 끝** — `Stairs.cs`(밟으면 회전 발동), `RoomClearGate.cs`(범위 내 적 전멸 시 계단 활성화, EnemyBun 미니언 포함해서 정확히 판정 확인). 방 클리어 → 계단 활성화 → 밟기 → 회전 → 새 면에서도 다시 정상 동작까지 실전 확인. **어제 보류했던 "구슬 획득 시 자동 회전?" 질문 해결** — 진짜 트리거는 계단이었음
- 🎉 **v0.2 마일스톤 전체 완료 (코드 + Unity 검증 다 끝남).**
- [x] **v0.3 착수 — 반복 층 나레이션 완료 + Unity 검증 끝** — `NarrationManager.cs`(글리치 텍스트, Hint/Confirmed 2단계), `CubeMapManager.MarkVisited()`에 실제 연결. Confirmed 문구 3종 랜덤 노출 확인. BGM 피치 왜곡은 오디오 에셋 없어서 보류
- [x] **보스 '포(鯆)' 1~2페이즈 완료 + Unity 검증 끝** — `BossPo.cs`(6색 공격 순환 + 2페이즈 위치 교란), `BossHealth.cs`(2페이즈 진입 시 강제 위치 교란 + 그동안 무적 — 27 전투 프레임 데이터 "페이즈 전환 무적" 스펙을 고정 시간 대신 실제 연출 길이로 구현), `DamageZone.cs`(장판), `PlayerController.ApplySlow()`(왜곡 디버프). "위치 교란"은 큐브 회전이 아니라 보스룸 내 순간이동으로 해석(6면 순환과 별개 씬이라). 7가지 공격 골고루 시전 확인. 3페이즈/부위 퍼즐/먹물 약점/처치 연출은 명시적으로 v0.3 범위 밖
- [x] **큐브 구조 노출 훅 + APV 최소 틀 완료 + Unity 검증 끝** — `CubeMapManager.OnCubeRevealTriggered`(N층 도달 시 발동) + `CubeRevealSequence.cs`(자리표시자 연출: 나레이션+페이드). 훅 발동 → 연출 시작/종료 로그 정확히 확인. 실제 Timeline/조명 작업은 아트 에셋 필요해서 보류, [[logic/APV 조명 설정]]에 나중 절차 정리
- 🎉 **v0.3의 시스템 코드성 작업 마무리 (코드 + Unity 검증 다 끝남).** 다음은 실제 레벨 콘텐츠/아트 에셋 도입에 달려있음
- [x] **1층 "방A 전투" 콘텐츠 완료 + Unity 검증 끝** — `EncounterSpawner.cs`(웨이브 기반 스포너, `RoomClearGate`와 자연스럽게 맞물림). 1웨이브(평 HP 절반)→2웨이브(평 정상 HP)→계단 활성화까지 실전 확인. 도중 "2웨이브 즉시 클리어" 버그 발견·해결(원인: Waves가 Project 프리팹이 아니라 Hierarchy에 남은 씬 오브젝트를 참조 — 자세한 진단 과정은 changelog 참고)
- [x] **흡(EnemyHeup) 회복 히스테리시스 완료 + Unity 검증 끝** — 회복 시작하면 100%까지 계속 회복(사용자 피드백: "왜 절반까지만 회복해?"). 공격 능력도 추가해봤다가 "체감이 안 된다"는 피드백으로 다시 제거 — 최종적으로 여전히 비공격 유닛
- [x] **세이브 & 로드 완료 + Unity 검증 끝** — `SaveData.cs`/`SaveManager.cs` 신규(JSON, `persistentDataPath`, 슬롯 1개). 층 입장 시 + 보스 처치 시 자동 저장, 게임 재시작 시 저장된 층/면/구슬 보유 그대로 이어서 시작하는 것까지 실전 확인
- [x] **4층 완전히 완료 + Unity 검증 끝** (벽 폭발 페널티 + 정상 클리어 두 시나리오 다) — 사용자 요청으로 "흡이 접근 전에 처치" 조건을 진짜 페널티(벽 폭발→즉사)로 구현. `PlayerHealth.OnDeath`/`ResetHealth()`, `CubeMapManager.ReloadCurrentFace()`(현재 면 재로드), `GameOverManager.cs`(⚠️ 원 기획 "처음부터"와 다르게 "죽은 층에서 다시 시작"으로 구현 — 사유 changelog 참고), `EnemyHeup.OnAbsorbStart`, `WallExplosionHazard.cs`(ShopSign이 회복 구역+폭발 벽 겸용). 실전 테스트로 "세이브된 HP 0으로 로드되면 영구히 안 죽는" 버그 발견·수정. 평×2+흡×1 전멸→`RoomClearGate`→계단→5층 회전까지 정상 확인
- [x] **3층 콘텐츠 완료 + Unity 검증 끝** (새 코드 없이 기존 `RoomClearGate`/`Stairs`/`EnemyPyeong` 재사용, `EnemyWon` 프리팹 최초 생성). `SC_Face_2` 신규 + Addressable/CubeFaceData_2/GameState 연결(회전 경로 0→4→2) 전부 확인. 부수 발견: 반복 층 재방문 시 `EncounterSpawner.autoStart`가 다시 발동하는 게 "반복 층" 설계와 정확히 맞아떨어지는 의도된 동작임을 확인(버그 아님)
- [x] **2층 콘텐츠 완료 + Unity 검증 끝** — `HiddenOrbSpawner.cs` 신규(`PaintableObject.OnPainted`/`ColorOrbPickup.OnPickedUp` 이벤트 추가). "바닥 타일 색 복원 → 숨겨진 구슬 등장 → 획득 시 계단 자동 활성화" 패턴, `RoomClearGate` 없이도 계단이 열리는 두 번째 방식. 실전 테스트로 버그 2개 즉시 수정(옛 RoomClearGate 잔재 제거, 구슬 스폰 높이 조정용 `spawnHeightOffset` 추가)
- [x] **먹괴음 - 광(5종 마지막) 완료 + Unity 검증 끝** — `EnemyGwang.cs` 신규(`EnemyPyeong` 구조 재사용, BT는 사거리 대신 쿨다운만으로 AOE 트리거, 바닥 경고 인디케이터 `DamageZone` 패턴 재사용). HP 60/공격력 15/이동속도 1.5f/s(14 밸런스 수치 시트 기준). `SC_Face_4`에서 단독 테스트: 추격→경고 원→회피 시 무피해/범위 내 피격 둘 다 확인, 처치까지 붓 3콤보 여러 번 필요(HP 반영 확인), 처치 시 기존 `RoomClearGate`/`Stairs` 훅에 코드 추가 없이 자동 연결됨까지 확인
- [x] **재방문 랜덤 인카운터 완료 + Unity 검증 끝** — `RandomEncounterSpawner.cs` 신규(6면 전부에 `RandomEncounterBatchTool`로 일괄 배치), 재방문마다 무작위 N마리(종류당 중복 최대 2) 스폰, `minVisitCount`로 1~6층 첫 방문은 보호. **덤으로 심각한 기존 버그 발견·수정**: 스폰된 오브젝트가 면 씬이 아니라 영구 씬(`SC_Game`)에 계속 쌓이던 버그(`SetActiveScene` 미호출 + `Instantiate()` 부모 미지정) — `CubeMapManager`/`EncounterSpawner`/`EnemyBun`까지 같이 고침. 이걸로 "7층에 어느 면을 배정할지" 문제 자체가 해소됨(층 번호 대신 재방문 여부로 판단)

## 세션 연속성 — changelog 인덱스

> **새 세션 필독:** 여기(CLAUDE.md)는 항상 "현재 스냅샷"만 담아 크기를 고정한다. 상세 이력은 `changelog/`에 **날짜별 파일로** 쌓는다 — 매 세션 자동으로 읽히지 않으므로 토큰 걱정 없이 계속 쌓아도 된다.
> 규칙: 의미 있는 작업이 끝나면 `changelog/YYYY-MM-DD_[변경대상].md`를 새로 만들고, 아래 최근 목록에 한 줄만 추가한다 (오래된 줄은 5개 넘어가면 위 항목부터 삭제 — 전체 이력은 changelog 폴더에 남아있으니 삭제해도 안전함).
> 다음 세션은: 이 목록에서 최근 1~2개만 훑고, 필요하면 해당 changelog 파일을 열어 상세를 본다. **전체를 다 읽을 필요 없음.**

### 최근 changelog (최신이 위)

- `2026-09-12_큐브면고정몹-공격판정검증.md` — 사용자 요구: "던전도 큐브 겉면 거리 같이 큐브 뷰의 느낌 — 대신 몹은 면과 면을 이동 못 하고, 캐릭터도 다른 면 몹은 공격 불가." 프로토타입에서 먼저 검증(사용자 선택). `CubeFaceLockedMob.cs`(faceNormal 고정, CubeFaceZone 미구독이라 구조적으로 다른 면 이동 불가) + `CubeFaceAttackTester.cs`(F키 공격, 같은 면+사거리 둘 다 만족해야 명중) 신규, `CubeFaceMobTestBuilder.cs`로 기존 골목 배치 안 지우고 테스트 몹만 추가. **버그 발견** — 표면 클램프가 캡슐 피벗을 `cubeHalfExtent`에 정확히 고정해서 캡슐 절반이 파묻힌 반구로 보이던 문제 발견·수정(`surfaceOffset` 필드 추가, `CubeSurfaceWalker`/`CubeFaceLockedMob` 둘 다 적용) — 지난 세션 "플레이어 키가 작아서 나무가 안 보인다" 리포트의 진짜 원인이었을 가능성 높음. Console 로그로 3가지 시나리오(다른 면 무시/같은 면 명중/사거리 밖 무시) 전부 확인, 몹 이동도 리플렉션 검증(다른 면일 때 이동량 0, 같은 면일 때만 접근하되 자기 면 못 벗어남) 완료.
- `2026-09-12_큐브카메라-줌기능.md` — 사용자가 실제 Play로 골목 환경을 걸어보다가 "플레이어 키가 작아서 나무가 안 보인다"고 리포트, 카메라 줌인/줌아웃 요청. `CubeSurfaceCamera.cs`에 마우스 휠 줌 추가 — `distance`/`height`는 그대로 두고 곱해지는 `zoom` 배율(0.4~3, 기본 1)만 휠로 조절, 원시 스크롤 델타 크기 대신 방향만 취해서 과도하게 안 튀게 함. **한계**: 캐릭터 자체 스케일("키") 문제는 `CubeSurfaceWalker`의 표면 클램프가 캡슐 중심을 표면에 고정하는 구조라 스케일 변경 시 그 로직도 재검토 필요 — 이번엔 손 안 댐.
- `2026-09-12_큐브골목환경-실배치.md` — 위 3종 에셋을 실제로 `SC_CubePrototype.unity` 큐브 윗면에 배치해보라는 요청. `CubeStreetDressingBuilder.cs` 신규 — Z축을 가로지르는 8m 폭 도로 스트립(4m 타일 10개), 양옆 경계석(5개씩), 그 바깥에 가로수 3종을 4.5m 간격+지터로 교대 배치(총 8그루). `ArtDressingUtils.MeasureBounds()` 실측 후 역산 관행 재사용, 20m 면 길이에 4m 타일이 나머지 없이 정확히 5등분됨. Scene View 스크린샷(측면+위)으로 확인 — 도로 줄눈/경계석 라인/가로수 3종 실루엣 차이 다 눈으로 구분됨. **한계**: 도로/경계석에 콜라이더 없음(플레이어가 좌표 직접 클램프 방식이라 불필요), 실제 Play 테스트는 사용자 직접 필요, 문(던전 입구) 연결은 여전히 미착수.
- `2026-09-12_골목환경에셋-도로나무경계석.md` — 큐브 겉면을 "거리"로 만들기 전 기초 에셋 제작 요청. `AlleyRoad_01.fbx`(4x4m 도로 타일, 미세 변위+줄눈)/`AlleyCurb_01.fbx`(경계석)/`StreetTree_01~03.fbx`(가로수 3종, 각각 수형·색 다름) 신규. **발견** — 사용자가 "몹 5마리 수준의 에셋 필요"라고 요청해서 확인해보니 PolyHaven/Sketchfab/Poly Pizza/Hyper3D 생성 전부 이 세션에서 비활성(에이전트가 직접 못 켬, Blender N패널에서 사용자가 켜야 함) — 몹 5종도 사실 외부 다운로드가 아니라 같은 프로시저럴 방식으로 제작된 것이었음. 사용자 선택대로 나무를 프로시저럴로 더 공들여 재제작(가지+불규칙 잎클러스터+트렁크 수직 홈). **버그** — 잎/트렁크 메쉬에 `shade_smooth()`를 안 걸어서 몹 모델(매끈매끈)과 달리 각져 보이던 것 발견·수정. **다양화** — 나무 제작 절차를 함수화해서 크기·가지 수·기울기·색이 다른 변종 2개(`StreetTree_02`/`03`) 추가 제작, 3종 전부 Unity 재확인 완료(rootRot=(0,0,0)). **기법 개선** — FBX export 시 `bake_space_transform=True` 옵션으로 축 보정을 메쉬에 직접 구워서, 이전 세션에서 겪었던 "루트 회전 잔여로 배치 시 넘어지는" 버그를 원천 차단 — 앞으로 모든 신규 Blender 에셋에 표준 적용할 것.
- `2026-09-12_큐브표면보행-프로토타입.md` — 사용자 제안: "SC_Face_0 멀티룸 던전을 큐브 전개도로 접어서, 그 안에서 플레이하고 싶다" + 넥슨 '에버플래닛'의 글로브 뷰를 큐브에 적용. 독립 프로토타입(`SC_CubePrototype.unity`)으로 검증. `CubeSurfaceWalker.cs`/`CubeSurfaceCamera.cs`/`CubeFaceZone.cs` 신규. **버그 4건 발견·수정**: ① CharacterController가 "자기 위 방향" 내부 가정 때문에 매 프레임 재정렬과 충돌 — CC 없이 transform 직접 조작으로 우회 ② 콜라이더 없이 속도만으로 접지시키면 큐브를 뚫고 지나감 — 이동 후 표면 좌표 직접 clamp ③ 실제 키보드 입력 연결을 깜빡해서 전혀 안 움직임 — `Keyboard.current`로 WASD 직접 읽기 추가(이 프로젝트 activeInputHandler=1이라 레거시 Input 불가) ④ **"같은 면 위에서 카메라만 돌려도 면이 90도씩 튐"** — 매 프레임 좌표로 "어느 축이 제일 큰지" 계산하던 방식이 불안정, 사용자 제안대로 면마다 트리거 존(`CubeFaceZone`)을 두고 `OnTriggerEnter`로만 면이 바뀌게 변경 → 이 과정에서 트리거 존을 스케일 10배인 큐브의 자식으로 붙여서 콜라이더 크기까지 10배로 부풀어 겹치던 버그도 발견·수정(스케일 없는 별도 부모로 교체). 마우스 좌우 궤도 회전도 추가(플레이어 forward와 독립적인 orbitForward 소유). **검증 방법론**: 트리거 이벤트는 좌표 이동만으론 발생 안 하고 실제 물리 시뮬레이션 스텝이 필요함을 확인 — `Physics.simulationMode=Script` + `Physics.Simulate()`로 수동 강제해서 검증. **한계**: 기존 멀티룸 던전 콘텐츠를 이 방식으로 어떻게 이식할지 미정(레벨 디자인 자체가 달라짐), 사용자 재테스트 필요.
> 5개 초과분(던전전투게이팅-강화, 방A바닥구멍버그-수정, 1층-멀티룸던전-파일럿, 6면환경아트-전체배치, 환경아트-1층첫배치 등)은 이 인덱스에서 빠졌지만 `changelog/` 폴더에는 그대로 남아있음 — 필요하면 파일명으로 직접 찾아 읽을 것.

### 지금 당장 다음에 할 일

- ⏳ **[2026-09-12 진행 중] 큐브 표면 보행 → 골목 환경 → 카메라 줌 → 면 고정 몹/공격 판정 검증.** [[changelog/2026-09-12_큐브표면보행-프로토타입]] → 골목 에셋([[changelog/2026-09-12_골목환경에셋-도로나무경계석]]) → 실배치([[changelog/2026-09-12_큐브골목환경-실배치]]) → 카메라 줌([[changelog/2026-09-12_큐브카메라-줌기능]])에 이어, **사용자가 던전 설계 방향을 확정**: "던전도 큐브 겉면 거리 같이 큐브 뷰의 느낌 — 대신 몹은 면과 면을 이동 못 하고, 캐릭터도 다른 면 몹은 공격 불가." 이 규칙을 프로토타입에서 먼저 검증 완료([[changelog/2026-09-12_큐브면고정몹-공격판정검증]]) — `CubeFaceLockedMob`/`CubeFaceAttackTester`로 검증, 덤으로 "플레이어 키가 작다"의 진짜 원인이었을 캡슐 파묻힘 버그(`surfaceOffset`)도 발견·수정. **다음 순서**: ① 검증된 규칙을 실제 던전(SC_Face_0 방A~D)에 적용 — 각 방을 큐브 면으로 재구성, 기존 `EnemyPyeong` 등 NavMeshAgent 기반 AI가 임의 면 법선에서도 동작하는지 확인 필요 ② 실제 `BrushWeapon`에도 같은 면 검사 반영 ③ 나머지 면 테마 배정.
- 🎉 **먹괴음 5종 전체 제작·연결 완료 + 실제 플레이 테스트로 발견된 버그 6건 수정 완료.** `Assets/Scenes/Enemy*.prefab` 5개가 실제 모델+애니메이션 재생. 플레이어 쪽 회전 버그 3건 + 순간이동 카메라 실종 1건 + 이동 가속/감속 부재([[changelog/2026-09-10_플레이어이동-가속감속추가]]) 수정 완료. 이번 세션 핵심 교훈 — ① 이전 변형종을 씬에서 안 지우고 작업하면 반드시 겹쳐 보임 ② 메쉬 정점을 world-space로 옮기면 리깅된 본도 같이 옮겨야 함 ③ export 전 전체 메쉬 transform_apply 매번 필수 ④ 선택 기반 연산자 쓰기 직전 항상 `select_all(DESELECT)` ⑤ 프리팹 편집 모드에서 `Renderer.bounds`는 월드 스페이스 ⑥ `NavMeshAgent.isStopped`/`velocity`는 `agent.isOnNavMesh` 확인 후 접근 ⑦ 리포트된 증상을 바로 버그로 믿지 말고 `MonoBehaviour.Update()`를 리플렉션으로 반복 호출해 실측할 것 ⑧ 재컴파일은 실행 중인 Play 모드를 끊는다 — 사용자가 테스트 중일 땐 미리 안내 ⑨ Cinemachine 추적 오브젝트를 `transform.position`으로 순간이동시킬 땐 `CinemachineCore.OnTargetObjectWarped()` 필수 ⑩ **이 자동화 환경은 `Time.frameCount`가 `eval` 호출 사이에 멈춰있어서 프레임 번호에 의존하는 시스템(Cinemachine 등)은 리플렉션으로 강제 스텝이 안 됨** — `MonoBehaviour.Update()`처럼 프레임 번호 비의존 메서드만 결정적 검증 가능. **A/D·락온 "대시 느낌" 이슈는 2026-09-10에 보류(사용자 판단: 현재 접근으로는 추가 개선이 어렵다고 결론) — 재시도 전에 반드시 사용자에게 먼저 확인할 것, 임의로 재작업 시작하지 말 것.** 이동 가속/감속 추가([[changelog/2026-09-10_플레이어이동-가속감속추가]])까지는 반영된 상태.
- ⚠️ **[2026-09-11 발견] 이 자동화 환경은 Play 모드에 진입해도 실제 시간이 흐르지 않을 수 있다** — `editor_play`로 정상 진입해도(`playMode: "playing"`) `Time.frameCount`가 2에서 멈춘 채 수십 초~분 단위 실제 시간이 지나도 안 올라감. `set_autotick`(이미 활성 상태였음)/`editor_focus` 둘 다 시도했지만 안 풀림, 콘솔 에러/Error Pause도 아님(로그 확인함). 원인 미상 — 이 세션의 진짜 실시간 Play 테스트(NavMesh 이동 등 네이티브 시뮬레이션 필요한 것)는 불가능한 상태로 보임. **Animator 파라미터/트리거처럼 `MonoBehaviour.Update()`/`Animator.Update()`를 리플렉션으로 반복 호출해 검증 가능한 것들은 여전히 유효**하지만, `NavMeshAgent`의 실제 이동처럼 네이티브 엔진 틱에 의존하는 건 이 방법으로도 검증 불가 — 막히면 바로 사용자에게 알릴 것, 원인 추적에 턴 낭비하지 말 것. **관련 발견(2026-09-11)**: `SceneView.pivot`/`rotation`/`size`를 직접 대입하고 `Repaint()`해도 `sv.camera.transform`이 지연 동기화되어 엉뚱한 화면이 캡처됨(같은 틱 정지 계열 문제로 추정) — 임시 GameObject를 원하는 위치/회전에 두고 `SceneView.AlignViewToObject()`를 쓰면 즉시 반영됨, 스크린샷 찍을 땐 이 방법 사용할 것
- 🎉 **[2026-09-11 완료] 환경 아트 6면 전체 배치** — 1층 파일럿([[changelog/2026-09-11_환경아트-1층첫배치]])에 이어 나머지 5면도 전부 배치 완료([[changelog/2026-09-11_6면환경아트-전체배치]]). 면-테마 배정 확정: Face0=정육점골목/빨강, Face1=뒷골목/보라, Face2=전봇대거리/노랑, Face3=약국상가/초록, Face4=목욕탕내부/파랑, Face5=지하상가/무채색.
- ⚠️ **[2026-09-11 확정] 플레이타임 스코프 결정 — MVR(3~4시간/8층) 채택.** 사용자가 "맵이 너무 작다, 면당 10분+"를 요청 → 조사 결과 [[29 개발 리스크 진단 & 스코프 컷 계획]]에 이미 "8층+보스, 3~4시간" MVR이 문서화돼 있어서 이걸 정식 목표로 채택(12층 풀비전 6~8시간은 나중 확장). 역산하면 **층당 평균 26~30분** — 자세한 근거는 [[changelog/2026-09-11_플레이타임-확장-스코프결정]]. **아직 19번/09번/29번 문서에 정식 반영 안 함 — 파일럿 검증 끝나면 반영할 것.**
- 🎉 **[2026-09-11 완료] 1층 멀티룸 던전 파일럿** — [[changelog/2026-09-11_1층-멀티룸던전-파일럿]]. 위 결정에 따라 1층을 방 4개(방A~D, 각 30x30=기존 3배) 체이닝 구조로 재시공. `MultiRoomFloor1Builder.cs`/`PaintCountGate.cs` 신규, `Floor1CorridorBuilder.cs` 수정(RoomEdgeX 5→15, 서쪽 문 옵션).
- ✅ **[2026-09-11 완료] 방A 바닥 구멍 버그 수정** — [[changelog/2026-09-11_방A바닥구멍버그-수정]]. `Floor1CorridorBuilder` 호출이 씬을 다시 여는 문제로 Ground 리스케일이 날아갔던 것 수정.
- ✅ **[2026-09-11 완료] 던전 전투 게이팅 강화** — [[changelog/2026-09-11_던전전투게이팅-강화]]. "각 방당 20초도 안 걸린다" 리포트 → 방A/C 서쪽 문이 안 잠겨있던 것 + `ColorWaveEffect` 반경(30) 때문에 방B 퍼즐이 무의미했던 것 둘 다 수정. 전 구간 `RoomClearGate`(신규 `doorsToOpen` 필드) 강제 게이팅 + `EncounterSpawner` 웨이브 전환. 스크립트 재실행 시 게이트 유실로 방A 소프트락 날 뻔한 버그도 발견·수정("부모 없는 RoomClearGate"로 원본 식별). **한계**: Pyeong 단독으로는 목표 시간(26~30분) 채우기 비현실적. **다음 작업(우선순위 순):** ① **사용자 재실측** — 이번엔 전투가 강제되니 시간이 얼마나 늘었는지, 소프트락 없는지 확인 ② 그래도 목표에 크게 못 미치면 전투량 늘리기보다 탐색/이동/환경 기믹 비중 확대 검토(사용자와 논의) ③ 검증되면 나머지 5면에 동일 구조 확산 ④ 발광 오브젝트를 `PaintableObject` 계열로 연동(다중 파츠 대응, `MaterialPropertyBlock` 기반) ⑤ [[11 셰이더 설계 - 색 복원 파동]] 확장 셰이더로 발광 오브젝트 머티리얼 교체 ⑥ 먹괴음 부위별 색 변경도 `MaterialPropertyBlock` 적용 ⑦ 19/09/29번 기획 문서에 스코프 결정 정식 반영

- ✅ **[2026-08-31 완료]** 스코프 컷됐던 항목(팝업 힌트 UI 6종, 발자국 이펙트, ESC 스킵 확인창, Tab Lock-On) 전부 구현·검증 완료 — 상세는 아래 "[2026-08-31 완료]" 항목들 참고
- **⚠️ Enemy 프리팹을 Waves 배열에 채울 때는 반드시 Project 창에서 드래그할 것** — Hierarchy의 씬 오브젝트를 잘못 참조하면 그 오브젝트가 파괴되는 순간 웨이브가 조용히 깨짐 (오늘 겪은 버그, [[changelog/2026-08-19_인카운터스포너-1층콘텐츠]] 참고)
- ⚠️ **`CubeMapManager`의 `Cube Reveal Floor Number`를 테스트용으로 3으로 낮춰뒀다면 12로 복구했는지 확인할 것**
- ✅ **[2026-09-02 완료]** 위 실측 실패 원인이던 보스 공격력/윈드업 1차 조정 완료 — `BossPo.cs` 공격력 1페이즈 20→12, 2페이즈 25→16, 윈드업 0.42초→0.58초. 테스트용으로 낮춰뒀던 보스 HP도 250→500(원안)으로 복구. `SC_Face_0.unity` 씬 인스턴스까지 동기화, 컴파일 에러 없음 확인. 근거: [[changelog/2026-09-02_보스밸런스-1차조정]]
- ✅ **[2026-09-02 완료]** 밸런스 조정 직후 사용자가 "보스 잡고 있는데 스테이지가 넘어가버렸어" 리포트 → `RoomClearGate.AnyEnemyAlive()`가 `EnemyHealth`만 찾고 보스 전용 `BossHealth`는 못 찾아서, 보스룸에 일반 적이 없으면 보스가 살아있어도 즉시 "클리어"로 오판해 계단이 열리던 진짜 버그 발견·수정. `BossHealth`도 검사 대상에 추가, 컴파일 에러 없음 확인. 근거: [[changelog/2026-09-02_보스룸-클리어게이트버그]]
- ✅ **[2026-09-02 2차 완료]** 이어서 "피가 안 줄어든다" 리포트 → `DebugCombatHud.cs` 신규(화면에 실시간 HP 표시)로 확인해보니 **데미지는 정상**(500→158까지 정확히 감소). 진짜 원인은 **죽으면 면 전체 리로드로 보스 HP가 500으로 완전 리셋**되고 `SpawnPoint`가 복도 입구라 재접근에도 오래 걸리는 것 — "리셋이 반복되니 체감상 안 줄어드는 것처럼 느껴짐". 대응: 보스 HP 500→250 재조정, `SpawnPoint`를 보스방 문 안쪽(3, 0.3, 0.83)으로 **테스트 전용 임시 이동**(⚠️ 튜토리얼 체크포인트 실측 끝나면 원위치 13.5, 0.3, 0.47로 되돌릴 것), `EncounterSpawner` 비활성 방어 코드 추가. "죽으면 보스 HP가 완전 리셋되는 게 설계 의도인지"는 별도 논의 필요 — 오늘은 안 건드림. 근거: [[changelog/2026-09-02_보스밸런스-1차조정]] "2차" 섹션
- ✅ **[2026-09-02 완료]** 재플레이 중 "보스가 위치 교란 썼는데 맵 밖으로 떨어져" 리포트 → `BossPo.CastDisplace()`의 순간이동 좌표가 방A(반경 5m)보다 큰 `displaceRadius`(8m) 탓에 벽 밖을 가리킬 수 있던 버그 발견·수정(좌표를 방 안전 범위 ±4.3m로 clamp). 컴파일 에러 없음 확인. 근거: [[changelog/2026-09-02_보스위치교란-맵밖버그]]
- 🎉 **[2026-09-02 완료]** 위 수정까지 다 반영한 뒤 재도전 — **보스 처치 2회 성공, DPS 로그 확보.** 1차 23.8초 / 2차 21.3초(둘 다 HP 250) — 평균 22.5초, 역산 교전 비율 ≈59%(문서 가정 40%보다 뚜렷이 높음). 두 표본이 근접해서 재현성은 확인됨. 2차 로그를 자세히 보니 **스킬(강타/흐름)을 실제로 시전했는데 `x 0대상`으로 보스한테 대미지가 전혀 안 들어간 구간 발견** — 기본 콤보(구체 판정)는 꼬박꼬박 맞는데 강타(대시+구체)/흐름(전방 박스)은 빗맞을 수 있음. 원인은 두 스킬 다 `transform.forward`(그 순간 시선 방향)만 보고 조준해서, 판정 로직 버그는 아니고 설계상 한계로 판단 → `GetAimDirection()` 추가해서 락온 중엔 타겟 방향으로 강제 조준하도록 개선. ✅ **실제 플레이로 검증 완료**(락온 후 강타/흐름/번쩍 전부 정확히 명중, HP 250→230→215→205). 상세: [[changelog/2026-09-02_색스킬-조준개선]].

- 🎉 **[2026-09-02 3번째 실측]** 조준 수정 확인한 판을 끝까지 진행 — **전투 시간 18.9초** (구슬 다 쓸 때까지 스킬 섞어 사용). 지금까지 3전 최속. 교전 비율 ≈70%(스킬 없던 1·2차의 59%보다 뚜렷이 높음 — 문서의 "스킬 섞으면 DPS 상승" 서술과 방향 일치).
- ✅ **[2026-09-02 확정]** 위 결과로 "500 HP도 부족할 수 있다"고 봤었는데, **사용자 확인: 이 보스(포/鯆)는 튜토리얼 보스라 "60~90초" 목표 대상이 아님.** HP를 올리는 재조정 안 함 — **HP 250·공격력 12/16·윈드업 0.58초로 최종 확정.** "60~90초" 목표는 더 나중 층의 진짜 보스전 기준으로 남겨둠. 근거: [[14 밸런스 수치 시트]] "보스 스탯 — 포(鯆), 튜토리얼 보스" 섹션. ✅ **[2026-09-02 완료]** 뒷정리도 끝냄 — `SpawnPoint` 원래 자리(13.5, 0.3, 0.47)로 복구, `debugAlwaysFullPlayerHpOnLoad` 기본값 true→false로 되돌림, `Cube Reveal Floor Number`는 이미 12로 정상이었음 확인. 컴파일 에러 없음 확인. **오늘 세션 완전히 종료.**
- ✅ **[2026-08-31 완료]** 1층 입장 복도/방A 관련 잔여 버그 2건 추가 발견·수정: (1) 장애물+벽 사이에 낀 순간 벽까지 뚫고 떨어지는 버그(바닥 뚫림 버그와 동일 원인 — `WallThickness` 0.3→2m), (2) 방A에 경계벽이 아예 없어서 보스전 중 물러나다 맵 밖으로 떨어지는 버그(신규 `BuildRoomABoundaryWalls()` — 복도 문만 남기고 3면+문옆 2조각 벽으로 밀폐). 둘 다 CLI 재빌드로 씬 반영, 컴파일 에러 없음 확인. 근거: `Floor1CorridorBuilder.cs`
- ⚠️ 테스트 전용 `PlayerHealth.debugInvincible` 체크박스 추가(2026-08-31, 기본 꺼짐) — **실측/배포 전에는 꺼져있는지 항상 확인할 것**
- ✅ **[2026-08-31 완료]** [[15 튜토리얼 설계]] "키 안내 팝업" 6종 **전부 연결 완료** (WASD 이동/Space 구르기/좌클릭 공격/1~6 스킬/ESC 메뉴 5종 + ESC 스킵 확인창) — 신규 `HintPopupManager.cs`(NarrationManager 페이드 패턴 재활용, `GameSystems`에 설치) + `TutorialCheckpoint.hintText`/`EncounterSpawner.showFirstAttackHint`/`ColorSystemManager.AddOrb` 훅. 도중 발견·수정한 버그 2건: (1) 체크포인트 트리거가 너무 얇아서(0.5m) 장애물에 닿아야만 힌트가 뜸(바닥/벽 뚫림과 같은 원인 — `CheckpointDepth` 0.5→2m), (2) `EncounterSpawner.autoStart=true`라서 씬 로드와 동시에 방A 전투가 시작돼 좌클릭 힌트가 WASD 힌트한테 바로 덮여써짐 — `autoStart` 끄고 "장애물 통과" 체크포인트에서 `StartEncounter()` 호출하게 변경. 추가로 **ESC 스킵 확인창**(신규 `TutorialSkipManager.cs`, `GameSystems`에 설치)도 구현 — `SettingsMenu`의 기존 Esc 사용과 충돌하던 것도 `SettingsMenu.Update()` 하나가 우선순위 판단하는 구조로 해결. 전부 실측 검증 완료(스킵창/계속하기/건너뛰기+전투시작/통과 후 정상 설정메뉴 4가지 다 확인). 추가로 **발자국 이펙트**(신규 `FootstepEffect.cs`, `Player`에 상시 부착)까지 구현·검증 완료 —
`cc.velocity`가 매 프레임 마지막 `Move()` 호출(중력, 수직 전용) 기준으로 갱신돼서 이동 감지가
항상 실패하던 버그도 발견·수정(프레임 간 위치 변화량 직접 측정으로 교체). Tab 락온도 간소화 버전(카메라 프레이밍 제외 — 타겟 감지/전환/회전/화면 마커)으로 구현·검증
완료 — 신규 `LockOnController.cs`(`Player`)/`LockOnMarkerUI.cs`(`GameSystems`),
`EnemyHealth`/`BossHealth`에 `IsDead` 프로퍼티 추가(시체 재타겟팅 방지), `PlayerController`가
락온 중엔 회전을 양보하도록 수정. 탐지/해제 반경은 방A 크기에 맞게 3.5m/5m로 튜닝.
**이걸로 [[15 튜토리얼 설계]] 스코프 컷됐던 4개 항목(팝업 힌트/ESC 스킵/발자국/Tab 락온) 전부
완료.** 락온 카메라 프레이밍(`CinemachineTargetGroup`, 문서 스펙)도 같은 날 시도했으나 4라운드
시행착오(비활성 오브젝트 Find 실패 → 벽에 낀 채 못 빠져나옴 → 마우스 켜니 고정 의미 없음 →
Deoccluder 붙이니 좁은 방에서 과확대) 끝에 **보류 결정** — `LockOnController`는 카메라를
아예 안 건드리도록 롤백(회전/마커/전환만 유지), 카메라 오브젝트들은 나중에 직접 튜닝할 사람을
위해 씬에 남겨둠(Priority -10, 안 뜸). 근거: [[changelog/2026-08-25_1층콘텐츠-입장복도]] 전체
- ⚠️ **[2026-08-31 발견 → 해결]** 세이브 파일(`save_slot_0.json`)이 다양한 실측 플레이로 `currentFaceIndex=4`, `playerHP=15`까지 진행돼있던 상태 확인 — `currentFaceIndex`는 0으로(백업: `save_slot_0.backup_face4.json`), `playerHP`는 15→100으로 복구(백업: `save_slot_0.backup_hp15.json`). 둘 다 정상화 완료.
- UI 착수 시 [[25 색맹 접근성 설계]] "패턴 강조 모드"부터 반영 (나중에 끼워넣지 말 것)

### ⚠️ 로컬 프로젝트 폴더 사고 (2026-08-16, 다음 세션 필독)

사용자의 Unity 로컬 프로젝트가 **git과 연결 안 된 별도 폴더(`C:\Users\user\Meokgoeeum`)**였다는 게 뒤늦게 발견됨 — GitHub 저장소는 `C:\Users\user\Documents\GitHub\Meokgoeeum`에 별도로 클론되어 있었고, 둘이 완전히 다른 폴더라 그동안 Claude가 push한 코드가 로컬 Unity에 전혀 반영되지 않고 있었음.

**해결:** 사용자가 Unity Hub에서 프로젝트를 `Documents\GitHub\Meokgoeeum`로 전환. 이 과정에서 이전 폴더에 했던 Cinemachine 씬 설정(카메라 GameObject, 컴포넌트 부착)이 전부 날아가서 **다시 설정함** (같은 실수들 — Brain이 자식 오브젝트로 들어가는 문제 — 또 겪었지만 가이드 문서 덕에 빠르게 해결). 재설정 후 `BrushWeapon`, `MouseSensitivitySetting`도 재부착 확인 완료.

**앞으로 이 프로젝트의 로컬 작업 폴더는 `C:\Users\user\Documents\GitHub\Meokgoeeum` 하나로 고정.** `C:\Users\user\Meokgoeeum`는 더 이상 사용 안 함 (사용자가 나중에 직접 삭제할 예정, Claude가 먼저 언급할 필요는 없음).

## v0.1 목표 (다음 작업)

```
① PlayerController — CharacterController + WASD + Cinemachine
② 붓 기본 공격 — 3타 콤보 (Animator)
③ 먹괴음 - 평 — NavMeshAgent + OverlapSphere AI
④ 색 구슬 드랍 & 획득
⑤ 색 복원 파동 — URP Shader Graph + VFX Graph
```

---

## 개발 원칙

### 답변 형식
- 한국어로 답변
- 단순 질문은 바로 코드로 가도 됨
- 빠른 프로토타이핑 흐름 우선
- 왜 해당 코드를 작성했는지 설명 필수

### 설계 우선순위
1. 혼자 빠르게 만들 수 있는가
2. 혼자 유지보수 가능한 복잡도인가
3. 디버깅이 쉬운가
4. 나중에 확장/개선 가능한가

### 설계 방식
- 단순한 버전 먼저, 필요하면 더 구조적인 버전 선택지 제시
- 더 나은 패턴이 있으면 제안해도 되지만 혼자 감당 가능한 수준으로

### 스크립트 기본값 변경 시 유의사항 (2026-08-16 추가)

- **`public` 필드의 기본값(`= 20f` 등)을 코드에서 바꿔도, 씬에 이미 배치된 오브젝트의 값은 자동으로 안 바뀐다.** Unity는 컴포넌트가 처음 추가될 때의 값을 그대로 직렬화해서 씬 파일에 저장하기 때문. (EnemyHealth maxHP, PaintableObject fadeDuration에서 반복된 패턴)
- 그래서 밸런스/타이밍 값을 코드에서 바꿀 때마다: **"이미 씬에 배치된 오브젝트도 Inspector에서 수동으로 값을 바꿔야 한다"는 걸 매번 명시적으로 안내할 것.** 빼먹으면 사용자가 "왜 안 바뀌지"하고 헷갈림

### 순차 효과 "타이밍이 안 보임" 버그 진단 원칙 (2026-08-18 추가)

- **여러 대상에 시간차를 두고 순차적으로 효과를 주는 시스템**(색 복원 파동처럼 거리순으로 퍼지는 연출, 나중에 만들 큐브 회전 연출·컷씬 등)에서 "순서 없이 동시에 일어나는 것처럼 보인다"는 리포트가 오면, **로직 버그로 단정하기 전에 먼저 이 비율부터 확인**: `개별 전환(페이드/트윈 등) 소요 시간` vs `대상 간 트리거 시간차`. 전자가 후자보다 길면 전환 구간이 서로 겹쳐서 육안상 동시로 보이는 것뿐 — 트리거 로직 자체는 정상일 가능성이 큼
- 실제 사례: [[changelog/2026-08-16_색복원파동-타이밍조정]] — 파동 트리거는 거리순으로 정확했지만(0.18/0.21/0.31/0.46s) `fadeDuration`(0.5s)이 트리거 시간차 총합(0.28s)보다 길어서 "버그"처럼 보였음
- 확인 절차: 진단 로그로 실제 트리거 시각을 먼저 찍어본다 → 트리거 자체가 순서대로면 로직은 정상, 튜닝(전환 시간 단축) 문제로 처리

### 피격/상태 변화 로깅 원칙 (2026-08-16 추가, 2026-08-19·2026-08-21 수정)

- **모든 `IDamageable.TakeDamage()` 구현체는 죽음 등 "드문" 상태 변화는 `Debug.Log`로 남긴다** — 비주얼(HP바 등)이 아직 없는 v0.1 단계에서는 콘솔이 유일한 확인 수단
- ⚠️ **예외 (2026-08-19):** 맞을 때마다(`TakeDamage` 호출마다) 찍던 "피격! 남은 HP: ..." 로그는 **제거함** — 전투가 격해지면 콘솔이 이 로그로 뒤덮여서 다른 시스템(예: `EncounterSpawner`) 디버깅이 어려워진다는 실사용 피드백 반영. `PlayerHealth`/`EnemyHealth` 둘 다 적용
- ⚠️ **예외 (2026-08-21):** `EnemyHealth.Heal()`의 "회복! 남은 HP: ..." 로그도 **제거함** — 애초엔 "회복은 드문 이벤트라 노이즈 아님"이라고 판단했지만, [[EnemyHeup]]이 흡수 중 **매 프레임** `Heal()`을 호출하는 걸 놓쳐서 실제로는 피격 로그만큼 도배됨. 죽음(`Die()`) 로그만 남음
- ⚠️ **예외 (2026-08-21):** `EnemyWon`의 "먹물 투척!" 로그(원거리 공격마다 찍힘)도 **제거함** — 같은 이유(전투 중 콘솔 도배)
- 이 원칙은 `TakeDamage`/`Heal` 같은 **체력 변화** 메서드뿐 아니라, **적 공격 시전처럼 전투 중 반복적으로 호출되는 모든 액션 로그**에 똑같이 적용됨 — 새 체력/전투 관련 스크립트(보스 등)를 만들 때: **드문 1회성 이벤트(죽음, 페이즈 전환)만** 로그로 남기고, **매 프레임/매 공격마다 반복 호출되는 메서드에는 로그를 넣지 말 것** — "이건 드물다"처럼 넘겨짚지 말고 실제 호출 빈도를 먼저 확인할 것

### 코드 최적화 원칙 (2026-08-16 추가)

> "무조건 복잡하게 최적화"가 아니라 **"공짜로 얻을 수 있는 효율은 처음부터 챙기고, 병목이 확실한 곳만 신경쓴다"**. 필러 4(혼자서 완성 가능한 범위)와 충돌하면 필러 4가 우선.

- **매 프레임 비용이 반복 곱해지는 곳(적 여러 마리 × Update 등)은 처음부터 신경쓴다** — 나중에 적 개체 수가 늘어나면 그대로 병목이 되므로, 처음 짤 때 폴링 주기 다운샘플링(예: 매 프레임 대신 0.1~0.2초 간격) 같은 선택지를 기본으로 고려
- **자주 생성/파괴되는 오브젝트는 처음부터 풀링 구조 고려** — 색 구슬, 분열형 소형 개체, VFX 파티클처럼 반복적으로 Instantiate/Destroy될 게 뻔한 것들은 나중에 갈아엎기보다 지금 오브젝트 풀로 짜는 게 오히려 더 쉬움
- **머티리얼 인스턴스는 `renderer.material` 직접 접근보다 `MaterialPropertyBlock` 우선 검토** — 특히 색 복원 파동처럼 여러 오브젝트에 개별 파라미터를 줘야 하는 경우, 배칭/GPU 인스턴싱과의 호환성 때문에 중요
- **매 프레임 GC 할당(`new`, LINQ, 클로저 캡처 등)은 피한다** — Update()/FixedUpdate() 안에서 특히
- **자료구조 선택은 "왜 이걸 골랐는지" 한 줄이면 충분** — 완벽한 알고리즘을 찾으려 시간 쓰지 말고, O(n²)이 될 뻔한 뻔한 패턴만 피하는 수준으로 충분
- 확실한 병목인지 애매하면: 일단 단순하게 짜고 주석으로 "나중에 프로파일링 필요"만 남겨둔다 (→ [[30 플레이테스트 & 밸런싱 검증 계획]] Unity Profiler 활용)

### 금지
- 혼자 유지보수 불가능한 복잡도의 설계 강요
- 팀 협업 전제의 불필요한 추상화
- 측정 없이 "느릴 것 같다"는 감으로 하는 과도한 최적화 (조기 최적화)

---

## Obsidian 연동 규칙

- **Vault 경로:** `C:\Users\user\Documents\Game_Dev`
- **프로젝트 노트 루트:** `C:\Users\user\Documents\Game_Dev\03_프로젝트\먹괴음 게임 기획\`

### 노트 추가 전 필수 절차
1. 같은 내용의 노트가 이미 있는지 Glob/Grep으로 먼저 확인
2. 있으면 읽어서 파악한 뒤 이어서 작성
3. 없으면 새로 생성

### 자동 생성 노트 규칙

**코드 변경 시 / 기획 문서 대량 변경 시**
- 파일: `changelog/YYYY-MM-DD_[변경대상].md`
- 내용: 변경된 파일/클래스(또는 문서), 변경 내용, 변경 이유
- 완료 후 CLAUDE.md의 "세션 연속성 — changelog 인덱스" 최근 목록에 한 줄 추가 (5개 넘으면 오래된 것부터 제거 — 상세는 changelog 폴더에 영구 보존됨)

**로직 설명 시**
- 파일: `logic/[로직명].md`
- 내용: 로직 개요, 선택 이유, 장단점

**리팩토링 필요 시**
- 파일: `refactor/[대상].md`
- 내용: 현재 문제, 방향, 예상 범위, 우선순위
