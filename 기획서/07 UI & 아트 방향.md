#기획 #먹괴음 #UI #아트 #디자인

# UI & 아트 방향

> 2026-05-29 재작성 (실시간 3D 액션 기준)
> 2026-09-09 추가 — 발광 오브젝트 톤 확정, 색-오브젝트 매칭 초안, 구조 제작 계획

## HUD 구성 (1920×1080)

```
┌─────────────────────────────────────────────────────────┐
│ [HP바 + 캐릭터 아이콘]           [층 표시: B3F]          │  ← 상단 (높이 60px)
│                                                         │
│                   ( 3D 게임 화면 )                       │
│                                                         │
│ [색 구슬 슬롯 × 5]          [스킬 쿨타임 아이콘 × 6]    │  ← 하단 (높이 60px)
│  ● ● ● ○ ○                  [1][2][3][4][5][6]          │
└─────────────────────────────────────────────────────────┘
```

- 상단 좌: HP 바 220px와 캐릭터 아이콘
- 상단 우: 층 표시. B3F 형식, Warm Yellow #FFE082
- 하단 좌: 색 구슬 슬롯 5개, 40×40px 원형
- 하단 우: 색 스킬 쿨타임 아이콘. 각 스킬 단축키 1~6 표시, 쿨타임 시 회색 오버레이

## 아트 방향

### 톤 방향

세계관 문서의 "조선 풍 현대 도시"를 어떤 톤으로 구현할지 확정했다.

처음에는 한국과 사이버펑크를 결합하는 방향으로 접근했지만 두 가지 이유로 순화했다. 이 문서가 이미 정한 레퍼런스인 Tunic과 Gris는 둘 다 차갑고 날카로운 디스토피아와는 결이 다르고, 이 게임은 힐링게임을 지향하므로 시안과 마젠타 네온, 각진 홀로그램 같은 사이버펑크 요소는 그 방향과 부딪힌다.

그래서 "조선 풍 도시 + 색을 되찾는 등불과 초롱" 톤으로 확정했다. 기존 UI 팔레트의 Warm Yellow #FFE082와도 자연스럽게 이어진다.

| 배제 | 채택 |
|------|------|
| 차갑고 날카로운 디스토피아 사이버펑크, 시안·마젠타 네온, 각진 홀로그램 | 한지 초롱, 처마 밑 등불, 창호지 안쪽에서 새어나오는 부드러운 빛 |

### 색채 설계

- **기본 상태:** 회색조 도시. 채도를 0으로 클램프하며 발광 오브젝트도 예외 없이 적용한다.
- **색 복원 후:** 선명하고 따뜻한 색감
- **번지는 질감:** 색이 각지게 켜지는 대신 칠해지듯 번지는 질감으로 표현한다. Gris를 참고해 파동 경계가 날카롭지 않고 부드럽게 스며드는 느낌을 낸다.
- **먹괴음:** 검정과 먹물 텍스처, 이동 시 먹물 흔적 파티클

### 발광 오브젝트 디자인 원칙

- 각진 네온튜브나 홀로그램 광고판은 쓰지 않는다.
- 한지 초롱, 처마 밑 등불, 창호지 안쪽에서 새어나오는 부드러운 빛을 기본형으로 삼는다.
- 무채색 상태에서도 발광체의 형태와 명암 대비는 뚜렷하게 유지한다. 채도가 없는 대신 형태로 존재감을 보완한다.

### 색-오브젝트 매칭 (확정, 2026-09-10)

[[04 색 구슬 시스템]]의 6색-스킬 매칭에 오브젝트를 붙였다. 정육점, 목욕탕, 가로등, 약국, 뒷골목 초롱까지 다섯이 모이면 "조선 풍 도시"의 동네 상점가 한 세트가 완성된다. 검정만 성격이 달라서, 발광 오브젝트를 따로 두지 않고 이펙트로 처리하기로 확정했다.

| 색 | 스킬 | 발광 오브젝트 |
|----|------|---------------|
| 빨강 | 강타 | 정육점, 포장마차 등불 |
| 파랑 | 흐름 | 목욕탕 굴뚝 네온사인 |
| 노랑 | 번쩍 | 가로등. 기준 팔레트 Warm Yellow #FFE082와 직결 |
| 초록 | 되살림 | 약국 네온사인 |
| 보라 | 왜곡 | 뒷골목 초롱 |
| 검정 | 먹물, 보스 특효 | 오브젝트 없음. 다른 5색은 "세계가 색을 되찾는다"는 개념이라 발광 오브젝트가 자연스럽지만, 검정은 "먹혀서 침식된다"는 반대 개념이라 성격이 다르다. 이미 복원된 오브젝트 위에 빛을 빨아들이며 덧씌워지는 네거티브 이펙트로 구현하고, 보스전 전용으로 한정한다 |

#### 제작 현황 (2026-09-10~)

각 색마다 기존 처마 지등/좌판 유리등과는 다른 실루엣으로 새로 제작한다.

- [x] 빨강 — 정육점/포장마차 등불: 둥근 초롱 형태, 허리띠 장식 링 + 바닥 술. `Assets/Models/Environment/Lanterns/RedLantern_01.fbx`
- [x] 파랑 — 목욕탕 굴뚝 네온사인: 굴뚝에 나선형 네온관을 감은 형태. 처음엔 캡을 원뿔대로 만들었다가 부적절해 보인다는 지적으로 납작한 원판으로 교체. `Assets/Models/Environment/Lanterns/BathhouseSign_01.fbx`
- [x] 노랑 — 가로등: 기둥 + 둥근 유리알 + 돔형 등갓. 처음엔 등갓을 원뿔로 만들어서 유리알 곡률과 안 맞아 옆으로 삐져나왔는데, 유리알과 같은 곡률(타원구 오프셋)로 다시 만들어 해결. `Assets/Models/Environment/Lanterns/StreetLamp_01.fbx`
- [x] 초록 — 약국 네온사인: 벽걸이 액자형 사인판 + 십자가 네온. 프레임을 처음엔 속이 꽉 찬 블록으로 만들어서 네온과 배경판이 그 안에 파묻혔던 것을 테두리만 남은 액자 형태로 재구성. `Assets/Models/Environment/Lanterns/PharmacySign_01.fbx`
- [x] 보라 — 뒷골목 초롱: 육각(청사초롱) 프레임에 종이 패널 6장 + 걸이 고리 + 바닥 술. `Assets/Models/Environment/Lanterns/AlleyLantern_01.fbx`

**5색 발광 오브젝트 전부 제작 완료.**

### 색감 기술 구현 방향

[[11 셰이더 설계 - 색 복원 파동]]의 `_WaveRadius` 기반 원형 파동 Lerp 셰이더를 그대로 확장한다. 네온 전용 셰이더를 새로 만들지 않고 Emission과 채도 채널만 추가한다.

- 기본 상태는 완전 무채색으로 클램프한다. 네온 계열 머티리얼도 예외를 두지 않는다.
- 파동의 Lerp 곡선은 선형 대신 ease-out으로 바꿔 색이 확 터지는 손맛을 낸다.

### 캐릭터

- 붓의 먹물이 전투 시 튀는 이펙트 (VFX Graph)
- 피격 시 회색 먹물이 묻는 시각 효과

### 환경

- 조선 풍 현대 도시: 콘크리트, 간판, 전봇대, 골목
- 회색 상태와 색 복원 상태 두 버전의 머티리얼이 필요하다. Shader Graph로 전환한다.
- 큐브 경계 구역: 기하학적 패턴의 벽면
- **환경 다양성 확정 (2026-09-10):** 6개 면 각각 완전히 다른 테마. 외부 3개(정육점 골목·전봇대 거리·뒷골목) + 실내 3개(약국 상가·목욕탕 내부·지하상가)로 구성, 5개는 위 색-오브젝트 매칭과 그대로 연결. 상세 배치는 [[05 맵 시스템 - 큐브 구조]] "6개 면 테마" 절과 [[19 층별 상세 설계]] 1~6층 참고

## 구조 제작 계획

건물을 벽, 지붕, 장식 부품으로 나누고 그리드와 소켓 규칙 기반 조립 방식으로 제작하고 맵을 구성한다.

- **핵심 모듈:** 기둥, 처마와 지붕, 한지창과 문, 벽체 패널, 기단. 이 중 지붕이 난이도와 재사용도가 가장 높다.
- **발광 레이어:** 초롱과 등불은 구조 모듈과 분리된 별도 프리팹으로 만든다. 같은 구조에 레이어 조합만 바꿔 변형을 만든다.

이 구조 제작 계획은 [[05 맵 시스템 - 큐브 구조]]의 맵 생성 방식과 이어진다.

### 1차 모듈 제작 완료 (2026-09-10)

정육점 골목 스타일 상가 건물을 기준으로 5개 핵심 모듈을 그리드 2m 단위로 제작했다. 기둥과 벽체를 건물 앞면(같은 Y 위치)에 맞춰 배치해, 옆에서 봤을 때 사이가 뚫려 보이는 문제를 해결했다. 기단과 지붕은 그리드 1칸짜리 조각으로 만들어서 옆으로 이어붙여도 단차 없이 연결되도록 했다 — 지붕 경사는 건물 길이(X) 방향이 아니라 깊이(Y) 방향으로만 기울어져서, 1칸으로 잘라도 단면이 동일하다.

- `Assets/Models/Environment/BuildingModules/Columns/Column_Straight_01.fbx`
- `Assets/Models/Environment/BuildingModules/Walls/Wall_Cell_01.fbx`
- `Assets/Models/Environment/BuildingModules/Windows/Window_Cell_01.fbx` — 프레임 + 십자 문살 + 유리 포함
- `Assets/Models/Environment/BuildingModules/Foundations/Foundation_Cell_01.fbx`
- `Assets/Models/Environment/BuildingModules/Roofs/Roof_Cell_01.fbx`
- `Assets/Models/Environment/BuildingModules/Building_StreetUnit_Demo.fbx` — 위 모듈로 3칸(벽-창문-벽)을 조립한 참고용 예시

모든 모듈은 바닥이 로컬 Z=0, 중심이 로컬 X=0에 오도록 정리해뒀다 — 기단 위에 기둥/벽(Z=0.2), 그 위에 지붕(Z=2.8)을 쌓는 식으로 Unity에서 붙이면 된다.

### 벽·창문 폭 조절 (2026-09-10, `ModularPanelWidth.cs`)

"건물 특성에 따라 벽/창문 길이를 조절하고 싶다"는 요청으로, 그리드 배수가 아닌 임의의 폭에도 대응할 수 있는 Unity 컴포넌트를 만들었다. `Assets/Scripts/Level/ModularPanelWidth.cs`.

원리는 UI의 9-slice와 같다. 자식 파츠를 세 역할로 나눈다.

| 역할 | 대상 | 폭이 바뀌면 |
|------|------|-------------|
| Stretch | 유리, 문살, 벽면(위/아래) | 늘어난 만큼 그대로 커짐 |
| Reposition | 좌우 세로 프레임 | 두께(스케일)는 고정, 바깥쪽으로만 밀림 |
| Fixed | 중앙 세로 문살 | 폭과 무관하게 그대로 |

기준 폭(`baseWidth`, 기본 2.0m = 그리드 단위)과 원하는 폭(`width`)의 차이만큼 Stretch 파츠는 스케일에 더하고, Reposition 파츠는 위치를 절반씩 밀어낸다. 비율(곱셈)이 아니라 차이(덧셈)로 계산하는 게 핵심 — 그래야 프레임 두께 같은 "폭이 바뀌어도 그대로여야 하는 값"이 같이 늘어나지 않는다.

**Unity에서 실제 검증 완료:** `Window_Cell_01`에 폭 2.0 → 3.0m로 늘려서 테스트. 프레임 두께(0.05m)는 그대로 유지되고, 유리·문살·벽면은 정확히 늘어난 만큼 커지고, 좌우 프레임은 대칭으로 바깥으로 밀려나는 것을 좌표값과 스크린샷 양쪽으로 확인했다. `Wall_Cell_01`은 파츠가 하나뿐이라 Stretch 규칙 하나만 걸면 된다.

이 작업 도중 모듈 export 좌표 버그도 하나 발견했다 — 처음 export한 모듈들이 로컬 원점이 아니라 데모 조립 당시의 절대 좌표(예: 창문 x=-3)를 그대로 갖고 있어서, 좌우 판정에 쓰는 부호 계산이 틀어졌다. Blender에서 각 모듈을 X 중심 0으로 재정렬해서 재출고했다.

### 6면 건물 모듈 제작 현황 (2026-09-10~)

[[05 맵 시스템 - 큐브 구조]] "6개 면 테마"에 맞춰, 6면 전부 서로 다른 건물 모듈을 새로 만들기로 확정했다(그리드 모듈 공유 대신 면마다 완전히 새로 설계).

- [x] 정육점 골목 (외부·빨강) — 1차 모듈 제작 완료. 기둥/벽체/창문/기단/지붕
- [x] 전봇대 거리 (외부·노랑) — 대표 구조물인 전봇대 완료: 원뿔대 몸체 + 나무 완목 + 애자 3개 + 변압기 통. `Assets/Models/Environment/Props/UtilityPole_01.fbx`
- [x] 뒷골목 (외부·보라) — 대표 구조물인 노출 배관+계량기 완료: 세로 배관 + 브라켓 4개 + 엘보 + 계량기 박스/다이얼. 사용자가 배관 끝이 ㄴ자로 잘려있는 게 의도한 건지 물어봐서, 의도(지면 근처에서 꺾여 밸브로 들어가는 모습)는 맞지만 마감이 없었던 걸 확인 — 십자 손잡이 밸브를 달아서 완성. `Assets/Models/Environment/Props/AlleyPipe_01.fbx`
- [x] 약국 상가 (실내·초록) — 대표 구조물인 아케이드형 유리 지붕 완료: 반타원 아치 유리 볼트 + 철골 리브 2개 + 지지 기둥 2개 + 바닥. 그리드 1m 단위라 옆으로 이어붙여 복도 길이를 늘릴 수 있음. `Assets/Models/Environment/BuildingModules/Roofs/ArcadeRoof_01.fbx`
- [x] 목욕탕 내부 (실내·파랑) — 대표 구조물인 타일 아치 출입구 완료: 타일 벽체 + 반원 아치 개구부 + 장식 트림 + 배수구. 사용자가 아치 양옆(기둥 위-윗블록 아래 사이)에 실제로 뚫린 구멍을 지적 — 아치 바깥 반지름이 기둥 폭을 다 못 덮어서 생긴 진짜 구멍이었음. 기둥을 전체 높이로 만들고 아치 곡선 위쪽만 루넷(반원 채움)으로 덮는 구조로 재설계해서 해결. `Assets/Models/Environment/BuildingModules/Windows/BathhouseArch_01.fbx`
- [x] 지하상가 (실내·무채색) — 대표 구조물인 터널형 콘크리트 볼트 완료: 양쪽 측벽 + 반원 볼트 천장 + 콘크리트 리브 2개 + 매입 형광등(하우징+튜브) + 바닥. 조명 하우징이 천장을 0.005m 뚫고 나가있던 것을 실측으로 발견·수정(천장 안쪽으로 1cm 여유를 두고 재배치). `Assets/Models/Environment/BuildingModules/Roofs/UndergroundVault_01.fbx`

**6개 면 대표 구조물 전부 제작 완료.**

**재사용 가능한 프리팹으로 저장 완료:** 매번 컴포넌트와 규칙을 다시 설정할 필요 없도록, 컴포넌트와 규칙이 이미 설정된 상태로 저장해뒀다.

- `Assets/Models/Environment/BuildingModules/Walls/Wall_Cell_Adjustable.prefab`
- `Assets/Models/Environment/BuildingModules/Windows/Window_Cell_Adjustable.prefab`

프리팹을 새로 꺼내서 `width` 값만 바꿔도 규칙 재설정 없이 바로 적용되는 것까지 확인했다(벽 2.0→4.0m, 창문 2.0→1.5m 축소도 프레임 두께 불변으로 정상 동작).

## 제작 도구

작업용 PC에서 Blender 5.2.1과 Blender MCP, Claude Code를 Orca 터미널에서 함께 사용한다.

## 맵 회전 연출 UI

- 회전 전: 화면 테두리 해당 구슬 색 파동 효과 (글로우)
- 중앙: 색 복원 텍스트 페이드인 ("붉은 기억이 되살아났다")
- 회전 중: 화면 0.3초 암전 + 큐브 아이콘 (로딩 표시 대체)
- 회전 후: 새 맵으로 페이드인

## 폰트 & 언어

- 기본 언어: 한국어 (영어 추후 추가)
- **메인 UI:** 나눔스퀘어라운드 — 둥글고 아기자기, 가독성 높음
- **연출용 텍스트:** 온글잎 밀기름체 — 손그림 느낌 (내레이션, 보스 대사)

## 스킬 UI

- 인벤토리 없음
- 하단 구슬 슬롯 클릭 → 사용 가능한 스킬 목록 팝업
- 단축키(1~6)로도 즉시 발동 가능
- 구슬 슬롯 길게 누르면 **잠금(Lock)** 토글 — 잠긴 슬롯은 작은 자물쇠 아이콘 오버레이, 최대 2개까지 동시 잠금 가능 (→ [[02 플레이어 시스템]] "5슬롯 vs 6색" 절)

## Unity & Blender 구현 참고

- UI 시스템: **Unity UI Toolkit** 또는 **uGUI (Canvas)**
- 쿨타임 표시: Image Fill Amount 애니메이션
- 글리치 텍스트 내레이션: DOTween으로 텍스트 노이즈 효과
- 발광 오브젝트와 건물 모듈은 Blender에서 제작한 뒤 Unity로 임포트한다.

## 다음에 확정할 것

- [x] **처마 지등 형태 확정 (2026-09-10)** — 둥근 한지 초롱이 아니라 사각 창살 프레임 + 지붕 갓 + 걸이 고리 구조로 확정. 무채색 상태에서도 지붕/프레임/패널 3단 명암으로 형태가 구분됨을 Blender에서 확인. `Assets/Models/Environment/Lanterns/Lantern_Eave_Box_01.fbx`
- [x] **좌판 유리등 형태 확정 (2026-09-10)** — 유리 원통 + 보호 케이지 + 4방향 와이어가 상단 그립 링으로 모이는 구조. 실제 사각유리등 유물 레퍼런스를 참고해 손으로 잡거나 고리에 걸 수 있는 크기의 그립 링으로 마감. `Assets/Models/Environment/Lanterns/GlassLamp_Stall_01.fbx`
- [x] **먹괴음 3D 형태 방향 확정 (2026-09-10)** — 인간형 먹물 골렘으로 확정. 골반-허리-가슴으로 잘록하게 잇는 몸통, 허벅지-종아리·위팔-아래팔로 관절이 나뉜 팔다리, 손발 끝은 물방울로 마무리. 글로시한 완전 무채색 재질 + 무표정한 흰 눈동자 2개. 기본형(평) 기준으로 제작, 나머지 4종(원/흡/분/광)은 이 베이스에서 변형 예정. `Assets/Models/Characters/Enemies/Pyeong/InkGolem_Pyeong_01.fbx`
  - **부위별 색 변화 구현 방식:** 각 파츠를 별도 오브젝트로 나눈 것과 별개로, 머티리얼 자체를 부위별로 6개(머리/몸통/왼팔/오른팔/왼다리/오른다리) 분리해뒀다. 관절마다 오브젝트가 여러 개라도, 같은 부위에 속한 오브젝트는 전부 같은 머티리얼을 공유하므로 그 머티리얼 하나만 바꾸면 부위 전체가 같이 바뀐다. [[03 먹괴음 - 적 설계]] 부위별 색 변화 표와 1:1 대응. Blender에서 6색으로 임시 착색해 실제로 부위별 독립 변경이 되는지 검증 완료
  - **Unity 구현 시 주의:** 머티리얼을 공유하는 구조라, 먹괴음이 여러 마리 동시에 존재할 때 `material` 직접 수정은 같은 색을 흡수한 다른 개체에도 전부 영향을 준다. 개체별로 독립된 색 상태를 가지려면 CLAUDE.md 코드 최적화 원칙대로 `MaterialPropertyBlock`으로 개체마다 따로 적용할 것
  - **뼈대(Armature) 추가 (2026-09-10):** "가만히 서서 공격하면 이상하다"는 지적으로 리깅 필요성 확인. 척추(골반-허리-가슴-목-머리)와 팔다리 관절마다 총 17개 본을 넣고, 각 블롭 오브젝트를 해당 본 하나에만 100% 가중치로 묶는 리지드 스키닝 방식 적용 — 각 부위가 이미 별도 오브젝트라 버텍스 페인팅 없이도 정확히 맞아떨어짐. 팔을 들어올리고 다리를 굽히는 테스트 포즈로 실제 독립 동작 확인, 레스트 포즈 복귀도 뒤틀림 없이 확인됨. **한계:** 부드러운 스키닝이 아니라 블롭이 통째로 본을 따라가는 방식이라, 팔꿈치처럼 크게 꺾이는 관절에서는 이음매에 틈이 보일 수 있음 — 완전히 매끄러운 관절이 필요해지면 메쉬를 합치고 버텍스 웨이트를 손으로 칠하는 작업이 추가로 필요
  - **Unity Humanoid 리깅 실제 검증 (2026-09-10):** 사용자가 보유한 에셋스토어 패키지(Kevin Iglesias, Human Melee Animations)로 실제 리타겟팅 테스트. Blender 본 이름을 Unity Humanoid 규격에 매핑해 Avatar를 유효하게 만들었지만, 1차 테스트에서 **메쉬가 뼈를 안 따라가는 심각한 버그 발견** — 블롭 오브젝트에 Transform(Location)이 미적용 상태로 남아있던 게 원인. 전체 메쉬에 `transform_apply`를 적용해 좌표를 베이크한 뒤 재수출하니 정상적으로 리타겟팅된 펀치 애니메이션이 재생됨을 확인. 앞/뒤 방향도 사용자 재확인 요청으로 정측면 직교 뷰로 검증 — 얼굴이 Unity 기본 정면(+Z)과 정확히 일치함을 확인(버그 아님). 상세: [[changelog/2026-09-10_먹괴음골렘-유니티리깅검증]]
  - **원(원거리형) 제작 완료 (2026-09-10):** 리깅된 `InkGolem_Pyeong_01`을 복제해 첫 변형종 제작. 몸을 전체적으로 가늘게(X/Y 0.72배) 다듬고, 오른팔 전체를 대포 형태(윗팔-팔꿈치 고리-아래팔-약실-포신-포구, 6파츠 연속 구조)로 완전히 교체 — 손에 포신만 덧붙이는 게 아니라 팔 자체를 갈아엎는 방식. 포신은 원거리 실루엣이 멀리서도 읽히도록 크게 키우고, 다리와 겹치지 않게 팔을 바깥으로 벌린 전투 포즈로 배치. 등에는 잉크 탄약 주머니 추가. 상세: [[changelog/2026-09-10_먹괴음골렘-원제작]]
  - **흡(흡수형) 제작 완료 (2026-09-10):** 같은 베이스에서 두 번째 변형종 제작. 양손을 손목 앵커 기준 약 2.6배로 확대하고 손바닥이 향하는 쪽 표면을 안쪽으로 당겨 오목하게 파서 "빨아들이는 손" 실루엣 구현, 팔을 어깨 기준 바깥으로 벌려 골반과 안 겹치게 배치. 팔 길이 자체는 평/원과 동일하게 유지해 흡수형답게 손만 과장되도록 함. **버그 재발·수정** — 원 때와 똑같은 "팔이 3개로 보인다" 리포트. 이번엔 Pyeong 잔재가 아니라, 이미 export·커밋까지 끝난 **Won이 원점에 그대로 남아있는 상태에서 Heup을 export 직전 같은 원점으로 되돌리며 다시 겹친 것**이 원인 — 실제 export 파일은 매번 Heup 오브젝트만 선택해서 내보냈기 때문에 파일 자체는 정상이었고(재export 전후 파일 크기 동일로 확인) 뷰포트 표시 문제였음. Won을 씬에서 제거해 해결. **추가로 본-메쉬 정렬 불일치 발견·수정** — 사용자가 Armature 본 오버레이(점선+다이아몬드)를 보고 "팔이랑 동기화 안 해도 되냐"고 질문, 확인해보니 팔 메쉬는 회전·확대시켰지만 그 밑의 본(UpperArm/Forearm/Hand)은 원래 위치 그대로 남아있었음 — 레스트 포즈에선 안 보이지만 애니메이션 시 손이 엉뚱한 축으로 돌 뻔한 잠재 버그, 본을 메쉬와 같은 각도로 재정렬해 해결. 상세: [[changelog/2026-09-10_먹괴음골렘-흡제작]]
  - **분(분열형) 제작 완료 (2026-09-10):** 같은 베이스에서 세 번째 변형종 제작. Head/EyeL/EyeR를 복제·축소(0.55배)해 왼쪽 어깨에서 돋아난 작은 두 번째 머리(눈 2개 포함)를 추가 — "죽으면 2마리로 갈라진다"는 컨셉이 직관적으로 읽히는 쌍두 실루엣. 새 Head2 본을 Chest에 부착해 메쉬와 정확히 같은 좌표·회전으로 정렬(흡 때 배운 본-메쉬 동기화 교훈 바로 적용, 처음부터 본까지 함께 배치). 작업 시작 전 이전 변형종(흡)을 씬에서 미리 제거해 겹침 재발 예방. 상세: [[changelog/2026-09-10_먹괴음골렘-분제작]]
  - **광(범위형) 제작 완료 (2026-09-10) — 4종 변형 전체 완료:** 마지막 변형종. 허리를 크게 둘러싼 가시 8개짜리 에너지 고리(토러스+콘 결합, Waist 본에 리지드 스키닝)로 "주변 범위 공격"이 실루엣만으로 읽히도록 제작. 사용자 요청으로 머리에도 같은 모티프의 가시 왕관(6개)을 추가해 통일감 있는 "발산형" 테마로 마무리. 새로 만든 파츠는 전부 기존 본(Waist/Head)에 부착해 새 본 없이도 정렬 불일치 없이 완성. 상세: [[changelog/2026-09-10_먹괴음골렘-광제작]]

**먹괴음 5종(평/원/흡/분/광) 전체 제작 완료.**

- [x] **원/흡/분 머리 장식 추가 (2026-09-10)** — 광의 가시 왕관처럼 나머지 3종에도 각자 테마에 맞는 고유 머리 장식 추가. 원=앞으로 기운 타겟팅 안테나 2개(가늘고 위로 기운 뾰족한 형태), 흡=안으로 말린 갈고리 뿔 2개(베지어 커브+베벨로 곡선 형태, "빨아들이는" 이미지와 통일감), 분=양쪽 머리(본체+분열 예고용 작은 머리) 각각에 작은 스파이크 1개씩. 평은 기본형이라 장식 없이 유지. **버그 발견·수정** — 흡 작업 중 뿔을 `curve → mesh` 변환(`bpy.ops.object.convert`)할 때, FBX import 직후 전체 오브젝트가 자동으로 선택된 상태가 그대로 남아있어서(변환 대상으로 의도한 뿔 2개 외에 캐릭터 전체 22개 메쉬가 같이 선택돼 있었음) convert 연산이 그 메쉬들의 Armature 모디파이어까지 전부 적용·제거해버림 — 재export한 파일 크기가 절반 이하로 작아진 것으로 발견. 22개 메쉬에 모디파이어를 재부착해 해결. 원(join만 사용)은 같은 위험이 없었음을 재import 검증으로 확인. 상세: [[changelog/2026-09-10_먹괴음골렘-머리장식추가]]
- [x] **먹괴음 5종 실제 게임플레이 프리팹 연결 완료 (2026-09-10)** — `Assets/Scenes/Enemy{Pyeong,Won,Heup,Bun,Gwang}.prefab` 5개(`RandomEncounterBatchTool`이 실제로 참조하는 라이브 프리팹)에서 플레이스홀더 캡슐 메쉬를 제거하고, 리깅된 실제 모델을 자식 오브젝트로 부착. 루트의 게임플레이 컴포넌트(CapsuleCollider·NavMeshAgent·EnemyHealth·EnemyXXX 스크립트)는 전부 그대로 유지해 기존 밸런스 수치·로직에 영향 없음. 플레이어 CharacterController와 5종 몹 플레이스홀더 전부 `height=2, radius=0.5`로 일관되게 맞춰져 있던 걸 확인하고 이 컨벤션을 유지하기로 함 — 모델 실측 높이(1.19~1.24m)에 맞춰 종별로 스케일(1.59~1.68배)을 계산해 2m 캡슐에 꽉 차도록 자동 배치, 발 위치가 캡슐 바닥(로컬 Y=-1)에 정확히 오도록 검증. Animator + 이미 설정된 Avatar도 자식에 부착. 5종을 한 화면에 나란히 배치해 스크린샷으로 실루엣·비율 최종 확인. 상세: [[changelog/2026-09-10_먹괴음5종-프리팹연결]]
- [x] **원/흡/분/광 Unity Humanoid Avatar 설정·검증 완료 (2026-09-10)** — 평에서 검증된 것과 동일한 17본 매핑(Hip→Hips, Waist→Spine 등)을 원/흡/분/광에도 그대로 적용 — 4종 모두 같은 본 이름 구조를 공유하므로 매핑을 그대로 재사용. `animationType=Human`, `avatarSetup=CreateFromThisModel`로 설정 후 `Avatar.isValid`·`isHuman` 확인. 레스트 포즈만으로는 알 수 없는 "실제로 리타겟팅되는지"를 숫자로도 검증 — 에셋스토어 펀치 애니메이션(`HumanM@AttackPunch01_R`)을 40% 지점에서 샘플링해 `Hand_L` 본 위치 이동량(0.52~0.62m)과 `UpperArm_L` 본 회전량(121~140도)을 레스트 대비 실측, 5종 전부 유의미하게 변형됨을 확인. 원 캐릭터로 펀치 포즈 스크린샷까지 찍어 시각적으로도 재확인(다리 굽힘·대포팔 휘두름 정상 표시). 상세: [[changelog/2026-09-10_먹괴음5종-유니티휴머노이드검증]]
- [x] **색-오브젝트 매칭 최종 확정 (2026-09-10)** — 정육점(빨강)·목욕탕 굴뚝(파랑)·가로등(노랑)·약국 네온사인(초록)·뒷골목 초롱(보라)으로 "동네 상점가" 세트 완성. 검정은 발광 오브젝트 없이, 복원된 오브젝트 위에 덧씌우는 네거티브 흡수 이펙트로 별도 처리하기로 확정 — 5색과 성격이 반대(회복 vs 침식)라 같은 틀에 억지로 맞추지 않음. 보스전 전용
- [x] **환경 다양성 확정 (2026-09-10)** — 6개 면 각각 완전히 다른 테마로 확정. 외부 3개(정육점 골목·전봇대 거리·뒷골목) + 실내 3개(약국 상가·목욕탕 내부·지하상가), 5개는 위 색-오브젝트 매칭과 그대로 연결. [[05 맵 시스템 - 큐브 구조]] "6개 면 테마" 절, [[19 층별 상세 설계]] 1~6층에 반영

**"다음에 확정할 것" 항목 전부 완료.** 다음 단계는 확정된 발광 오브젝트·색-오브젝트·면 테마를 실제 Blender 에셋으로 제작하는 것.

## 관련 노트

- [[00 게임 개요]]
- [[03 먹괴음 - 적 설계]]
- [[04 색 구슬 시스템]]
- [[05 맵 시스템 - 큐브 구조]]
- [[08 사운드 기획]]
- [[11 셰이더 설계 - 색 복원 파동]]
- [[23 UI 디자인 기획서 (Stitch AI용)]]
